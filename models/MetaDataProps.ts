export default interface MetaDataProps{
    metaData:{
       title: string;
       description: string;
       keywords: string;
       og:{
        title:string,
        description: string,
       }
    }
}